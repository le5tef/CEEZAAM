export const validateLastName = (s: string) => {
  return s.length > 0
}
export const validateFirstName = (s: string) => {
  return s.length > 0
}
export const validatePatronymic = (s: string) => {
  return s.length > 0
}
export const validateType = (s: string) => {
  return s.length > 0
}
export const validateSeatsQuantity = (s: string) => {
  return s.length > 0
}
export const validateName = (s: string) => {
  return s.length > 0
}
